pkgname=treeos-base
pkgver=1.0
pkgrel=1
pkgdesc="Base metapackage for TreeOS"
arch=('any')
url="https://github.com/liamcodezz/treeos-base"
license=('MIT')
depends=(
  'git'
  'neovim'
  'zsh'
  'oh-my-zsh-git'
  'powerlevel10k'
  'base-devel'
  'ufw'
)
provides=('treeos-base')
conflicts=()
replaces=()
source=()
md5sums=()

package() {
  echo "This is a metapackage, nothing to install directly."
}
